name: CI
on:
  push:
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: foo
        run: |
          echo foo
          iwr -useb raw.githubusercontent.com/agate-pris/dotfiles/hello/.bin/bootstrap.ps1 | iex
        shell: powershell
